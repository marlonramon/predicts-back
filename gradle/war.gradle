apply plugin: "war"

bootWar {
    mainClass = "com.marlonramon.predicts.PredictsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
